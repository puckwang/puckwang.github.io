language: go

go:
  - master

before_install:
 - go get github.com/gohugoio/hugo

script:
 - hugo

deploy:
  provider: pages
  target-branch: master
  local-dir: public
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: develop
